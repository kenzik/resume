# Cloudflare Pages redirects
# https://developers.cloudflare.com/pages/configuration/redirects/

# Static assets are served automatically before these rules
# This SPA fallback only applies to routes that don't match static files
/*  /index.html  200
